<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:background="@color/white"
              android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/image_cell_grid_product"
            android:layout_width="match_parent"
            android:layout_height="@dimen/image_cell_grid_movie_width_height"
            android:layout_gravity="center_horizontal"
            android:scaleType="centerCrop"
            app:srcCompat="@android:drawable/ic_menu_help"/>

        <ImageView
            android:id="@+id/image_wish"
            android:layout_width="@dimen/image_wish"
            android:layout_height="@dimen/image_wish"
            android:layout_gravity="top|right"
            android:background="@color/transparent"
            android:src="@drawable/ic_wishlist_deselected_black"/>

    </FrameLayout>

    <TextView
        android:id="@+id/text_movie_product"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/default_margin"
        android:background="@color/white"
        android:clickable="true"
        android:gravity="center_horizontal"
        android:padding="@dimen/default_padding"
        android:textStyle="bold"/>

</LinearLayout>